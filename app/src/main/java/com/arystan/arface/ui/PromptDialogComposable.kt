package com.arystan.arface.ui

import androidx.compose.runtime.Composable

/** Wave 0 stub. ai-agent replaces with AlertDialog for prompt input. */
@Composable
fun PromptDialog(vm: ARViewModel) {
    // intentionally empty — dialog is conditional, agent renders nothing when not visible
}
